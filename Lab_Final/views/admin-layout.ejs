<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edenrobe Admin Panel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <style>
        :root {
            --edenrobe-navy: #1c2b4a;
            --edenrobe-light-gray: #f4f6f9;
            --edenrobe-accent: #e74c3c;
            --edenrobe-text: #333;
        }

        * {
            transition: all 0.3s ease;
        }

        body {
            background-color: var(--edenrobe-light-gray);
            font-family: 'Arial', sans-serif;
            color: var(--edenrobe-text);
        }

        .admin-wrapper {
            display: flex;
            min-height: 100vh;
        }

        .vertical-menu {
            width: 80px;
            background-color: var(--edenrobe-navy);
            padding-top: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            transition: width 0.3s ease;
            overflow: hidden;
            position: fixed;
            left: 0;
            top: 0;
            bottom: 0;
            z-index: 1000;
        }

        .vertical-menu:hover {
            width: 250px;
        }

        .vertical-menu:hover .menu-text {
            opacity: 1;
            visibility: visible;
        }

        .menu-item {
            width: 100%;
            display: flex;
            align-items: center;
            padding: 15px;
            color: white;
            text-decoration: none;
            position: relative;
        }

        .menu-item:hover {
            background-color: var(--edenrobe-accent);
        }

        .menu-item .material-icons {
            margin-right: 15px;
            min-width: 24px;
        }

        .menu-text {
            opacity: 0;
            visibility: hidden;
            white-space: nowrap;
        }

        .header {
            background-color: white;
            padding: 15px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            position: fixed;
            top: 0;
            left: 80px;
            right: 0;
            z-index: 999;
        }

        .main-content {
            margin-left: 80px;
            margin-top: 80px;
            padding: 20px;
            width: calc(100% - 80px);
        }

        .vertical-menu:hover + .header {
            left: 250px;
            width: calc(100% - 250px);
        }

        .card-hover {
            transform: translateY(0);
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        .card-hover:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.15);
        }

        @media (max-width: 768px) {
            .vertical-menu {
                width: 0;
            }
            .main-content {
                margin-left: 0;
                width: 100%;
            }
            .header {
                left: 0;
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="admin-wrapper">
        <div class="vertical-menu">
            <a href="/admin/dashboard" class="menu-item">
                <i class="material-icons">dashboard</i>
                <span class="menu-text">Dashboard</span>
            </a>
            <a href="/admin/products" class="menu-item">
                <i class="material-icons">inventory</i>
                <span class="menu-text">Products</span>
            </a>
            <a href="/admin/categories" class="menu-item">
                <i class="material-icons">category</i>
                <span class="menu-text">Categories</span>
            </a>
            <a href="/admin/locator" class="menu-item">
                <i class="material-icons">location_on</i>
                <span class="menu-text">Locator</span>
            </a>
            <a href="/" class="menu-item">
                <i class="material-icons">language</i>
                <span class="menu-text">Main Website</span>
            </a>
            <a href="/admin/logout" class="menu-item">
                <i class="material-icons">logout</i>
                <span class="menu-text">Logout</span>
            </a>
        </div>
        

        <div class="header">
            <div class="d-flex align-items-center">
                <img src="/Images/black_logo.png" alt="Edenrobe logo" height="40">
            </div>
            <div class="d-flex align-items-center">
                <h4 class="mb-0 me-3">Admin Panel</h4>
                <button class="btn btn-outline-dark">
                    <i class="material-icons">person</i>
                </button>
            </div>
        </div>

        <main class="main-content">
            <%-body  %>
        </main>
    </div>
</body>
</html>